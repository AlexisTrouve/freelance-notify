# Additional requirements for Upwork adapter
# Install with: pip install -r requirements-upwork.txt

# Camoufox - stealth Firefox (bypasses Cloudflare)
# Install separately: pip install "camoufox[geoip]" && python -m camoufox fetch
camoufox>=0.4.0
camoufox-captcha>=0.1.0

beautifulsoup4>=4.12.0
requests>=2.31.0
